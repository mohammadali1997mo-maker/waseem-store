rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // تسمح هذه القواعد بالوصول الكامل دون تسجيل دخول لمساعدة الشريك في سوريا لتخطي حظر غوغل
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
